export interface Medicine {
  id: string;
  type: string;
  name: string;
  quantity: string;
  timing: string;
  instruction: string;
}

export interface Investigation {
  id: string;
  text: string;
}
